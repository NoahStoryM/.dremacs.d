;; -*- lexical-binding: t -*-

(meta-import (private packages eshell-syntax-highlighting)
             (private packages capf-autosuggest)
             (private packages eat))

(meta-export (private layers term packages))
