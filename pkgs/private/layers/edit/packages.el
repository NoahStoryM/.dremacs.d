;; -*- lexical-binding: t -*-

(meta-import (private packages avy)
             (private packages consult)
             (private packages embark)
             (private packages wgrep))

(meta-export (private layers edit packages))
