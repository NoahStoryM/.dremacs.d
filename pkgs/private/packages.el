;; -*- lexical-binding: t -*-

(meta-import (private packages emacs))
(meta-import (private packages theme))
(meta-import (private packages modeline))
(meta-import (private packages marginalia))
(meta-import (private packages vertico))
(meta-import (private packages autorevert))
(meta-import (private packages savehist))
(meta-import (private packages which-key))
(meta-import (private packages orderless))
(meta-import (private packages display-line-numbers))
(meta-import (private packages hl-line))
(meta-import (private packages simple))
(meta-import (private packages tab-bar))
(meta-import (private packages tab-line))

(meta-export (private packages))
